@model IEnumerable<MovieStoreV4.Models.tbl_Category>
@{ 
        Layout = null;
}

<h4>Kategoriler</h4>
@foreach (var item in Model) { 

<div class="row">
    <div class="col-lg-6">
        <ul class="list-unstyled">
            <li>
                @*@Html.ActionLink(item.Name ,"CategorizedMovie","Movies",new {categoryId=item.ID })*@
                <a href="/Movies/CategorizedMovie/@item.ID">@item.Name (@item.tbl_Movie.Count())</a>
            </li>
            
        </ul>
        </div>
    </div>
}